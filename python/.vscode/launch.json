{
    "configurations": [
        {
            "type": "python",
            "request": "launch",
            "name": "Launch With Python",
            "program": "${workspaceFolder}/emulator.py"
        }
    ]
}